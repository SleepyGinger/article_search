# article_search

Using the Readability API article_search extracts only the text from
a given url from link.csv and counts how many times a specific
string is mentioned. It then extracts the whole DataFrame to a .xlsx
